---
/**
 * Sitewide footer — privacy reassurance line, nav columns, trust pages,
 * GitHub link, copyright. Trust-page links (/about, /privacy, /terms,
 * /contact) are wired now so they're ready when Phase 0 commit 3 ships them.
 *
 * All strings from site.config.ts.
 */
import { site } from "../site.config";

const year = new Date().getFullYear();

// Trust pages live here now that they exist (shipped Phase 0 commit 3).
interface FooterLink {
  href: string;
  label: string;
  external?: boolean;
}
interface FooterColumn {
  title: string;
  links: FooterLink[];
}

const columns: FooterColumn[] = [
  {
    title: "Product",
    links: [
      { href: "/app/", label: "Open eraser" },
      { href: "/how-it-works/", label: "How it works" },
      { href: "/compare/", label: "Compare" },
      { href: "/faq/", label: "FAQ" },
    ],
  },
  {
    title: "Company",
    links: [
      { href: "/about/", label: "About" },
      { href: "/contact/", label: "Contact" },
      { href: site.githubRepo, label: "GitHub", external: true },
    ],
  },
  {
    title: "Legal",
    links: [
      { href: "/privacy/", label: "Privacy Policy" },
      { href: "/terms/", label: "Terms" },
    ],
  },
];
---

<footer class="border-t border-[var(--color-border)] bg-[var(--color-bg-subtle)]">
  <div class="mx-auto max-w-6xl px-4 py-12 sm:px-6">
    <div class="flex flex-col gap-10 md:flex-row md:justify-between">
      {/* Brand + reassurance */}
      <div class="max-w-xs">
        <a href="/" class="flex items-center gap-2 font-bold tracking-tight">
          <span
            class="inline-flex size-7 items-center justify-center rounded-md text-[var(--color-accent)]"
            style="background-color: color-mix(in srgb, var(--color-accent) 14%, transparent);"
            aria-hidden="true"
          >
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="17"
              height="17"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="m7 21-4.3-4.3c-1-1-1-2.5 0-3.4l9.6-9.6c1-1 2.5-1 3.4 0l5.6 5.6c1 1 1 2.5 0 3.4L13 21"></path>
              <path d="M22 21H7"></path>
              <path d="m5 11 9 9"></path>
            </svg>
          </span>
          <span>{site.name}</span>
        </a>
        <p class="mt-3 text-sm leading-relaxed text-[var(--color-fg-muted)]">
          Erase objects from photos right in your browser. Your images never
          leave your device — no upload, no signup, no watermark.
        </p>
      </div>

      {/* Link columns */}
      <div class="grid grid-cols-2 gap-8 sm:grid-cols-3">
        {
          columns.map((col) => (
            <div>
              <h2 class="text-sm font-semibold text-[var(--color-fg)]">
                {col.title}
              </h2>
              <ul class="mt-3 space-y-2">
                {col.links.map((link) => (
                  <li>
                    <a
                      href={link.href}
                      class="text-sm text-[var(--color-fg-muted)] transition-colors hover:text-[var(--color-fg)]"
                      {...(link.external
                        ? { target: "_blank", rel: "noopener noreferrer" }
                        : {})}
                    >
                      {link.label}
                    </a>
                  </li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>
    </div>

    <div
      class="mt-10 flex flex-col gap-3 border-t border-[var(--color-border)] pt-6 text-sm text-[var(--color-fg-subtle)] sm:flex-row sm:items-center sm:justify-between"
    >
      <p>© {year} {site.name}. Free forever.</p>
      <p class="inline-flex items-center gap-1.5">
        {/* Lucide "shield-check" — privacy reassurance */}
        <svg
          xmlns="http://www.w3.org/2000/svg"
          width="14"
          height="14"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          aria-hidden="true"
        >
          <path d="M20 13c0 5-3.5 7.5-7.66 8.95a1 1 0 0 1-.67-.01C7.5 20.5 4 18 4 13V6a1 1 0 0 1 1-1c2 0 4.5-1.2 6.24-2.72a1.17 1.17 0 0 1 1.52 0C14.51 3.81 17 5 19 5a1 1 0 0 1 1 1z"></path>
          <path d="m9 12 2 2 4-4"></path>
        </svg>
        100% on-device · nothing uploaded
      </p>
    </div>
  </div>
</footer>
